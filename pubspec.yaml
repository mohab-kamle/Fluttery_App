name: flutter_at_akira_menai
description: "still waiting for the next akira menai"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  path_provider: ^2.1.1
  http: ^1.3.0
  firebase_core: ^3.13.0
  flutter_dotenv: ^5.2.1
  shared_preferences: ^2.5.3
  firebase_auth: ^5.5.2
  google_sign_in: ^6.3.0
  provider: ^6.1.4
  image_picker: ^1.0.7
  permission_handler : ^12.0.0+1
  path: ^1.9.1
  hive_flutter: ^1.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - .env
